button.secondary-link {
  background: none;
  border: none;
  padding: 0;
	color: inherit;
  text-decoration: none;
	outline: inherit;
	font: inherit;
	font-size: inherit;
  cursor: pointer !important;
}

button.secondary-link:hover {
    color: inherit;
}
